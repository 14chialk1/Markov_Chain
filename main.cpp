#include <iostream>
#include "markov.h"
using namespce std;
words = ["the", "cat", "sat", "down"]
int main(){
    std<<cout::int readWordsFromFile(std::string filename, std::string words[], int maxWords)
}